<div class="waypoint-nav space"></div>
<%= render partial: 'shared/nav' %>
<div class="picture">
  <%= image_tag @picture.image.url(:full_size), class: 'center' %>
  <h1><%= @picture.name %></h1>
</div>
